<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <artifactId>paas-metarepo-service-server</artifactId>
    <packaging>jar</packaging>

    <parent>
        <groupId>com.hongyang.platform</groupId>
        <artifactId>paas-metarepo-service</artifactId>
        <version>${revision}</version>
    </parent>

    <dependencies>
        <!-- 内部 core 模块 -->
        <dependency>
            <groupId>com.hongyang.platform</groupId>
            <artifactId>paas-metarepo-service-core</artifactId>
        </dependency>

        <!-- Spring Boot Web（排除 Tomcat，使用 Undertow） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-undertow</artifactId>
        </dependency>

        <!-- Spring Cloud -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
        </dependency>

        <!-- DAO 框架（包含 MyBatis-Plus + 租户拦截 + 审计填充 + ID 生成 + 缓存注解） -->
        <dependency>
            <groupId>com.hongyang</groupId>
            <artifactId>framework-dao</artifactId>
        </dependency>
        <dependency>
            <groupId>com.hongyang</groupId>
            <artifactId>framework-log4j2</artifactId>
        </dependency>
        <!-- 数据库驱动 -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>

        <!-- Redis + Caffeine 缓存（autoconfigure 传递依赖 client + core） -->
        <dependency>
            <groupId>com.hongyang</groupId>
            <artifactId>framework-redis-spring-boot-autoconfigure</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>

        <!-- MQ -->
        <dependency>
            <groupId>com.hongyang</groupId>
            <artifactId>framework-mq-client</artifactId>
        </dependency>

        <!-- 生命周期 -->
        <dependency>
            <groupId>com.hongyang</groupId>
            <artifactId>framework-lifecycle-spring-boot-autoconfigure</artifactId>
        </dependency>

        <!-- 工具 -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.fastjson2</groupId>
            <artifactId>fastjson2</artifactId>
        </dependency>
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
